#include <stdio.h>
#include <math.h>

int main() 
{
    int x, y, z; 
    printf("Enter base value:");
    scanf("%d", &x);
    
    printf("Enter power value:");
    scanf(" %d", &y);
    
    z=pow(x,y); 
    
    printf("%d ^ %d = %d",x,y,z);
    
    

    return 0;
}
