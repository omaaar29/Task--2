#include <stdio.h>
#include <math.h>

int main() 
{
    double x, y;
    
    printf("Enter the number:");
    scanf("%lf", &x);
    y=sqrt(x);
    
    printf("the root is %lf",y );
    

    return 0;
}
